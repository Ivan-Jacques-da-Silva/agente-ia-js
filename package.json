{
  "name": "agente-ia-js",
  "private": true,
  "workspaces": [
    "front",
    "api",
    "agente"
  ],
  "scripts": {
    "instalar": "npm i --workspaces",
    "dev": "node node_modules/concurrently/dist/bin/concurrently.js -k -n api,agente,front -c yellow,cyan,magenta \"npm run dev --workspace api\" \"npm run dev --workspace agente\" \"npm run dev --workspace front\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.1.0"
  }
}
